#contenedorbtnAtras{
    max-width: 414px;   /* Ancho máximo de 414px */
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 50px;
    background-color: rgba(255, 0, 0, 0.253);
    /*position:fixed; /* Fija el contenedor */
    top: 0; /* Ubica el contenedor en la parte inferior */
    left: 0; /* Alinea el contenedor a la izquierda */
    right: 0; /* Alinea el contenedor a la derecha */
    z-index: 1000; /* Asegura que esté encima de otros elementos */
    margin-bottom: 15px; /* Elimina el margen inferior */   /* Centrado en la pantalla */
}

#btn-atras {
    position: absolute; /* Para que se pueda posicionar dentro del contenedor */
    z-index: 10; /* Asegura que esté por encima de la imagen */
    top: 20px; /* Ajusta según la posición deseada */
    left: 20px; /* Ajusta según la posición deseada */
    background: none; /* Opcional, elimina fondo por defecto del botón */
    border: none; /* Opcional, elimina el borde del botón */
    cursor: pointer; /* Cambia el cursor a uno de clic */
}

/*#btn-atras {
    position: absolute; /* Para que se pueda posicionar dentro del contenedor */
   /* z-index: 10; /* Asegura que esté por encima de la imagen */
    /* top: 20px; /* Ajusta según la posición deseada */
    /*left: 20px; /* Ajusta según la posición deseada */
    /*background: none; /* Opcional, elimina fondo por defecto del botón */
    /*border: none; /* Opcional, elimina el borde del botón */
    /*cursor: pointer; /* Cambia el cursor a uno de clic */
/*}*/

#btn-atras_logo{
    width: 2rem; 
    border-radius: 15px;
    background-color: rgb(255, 255, 255);
}
#contenedor_img_producto{
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: auto;
    background-color: var(--mi-color-principal);
    /*border-radius: 0 0 180px 180px;*/
    margin-top: 0;
    margin-bottom: 0.8rem;
}
#imagen-producto{
    width: 25rem;
    height: auto;
    margin-top: 7px;
    margin-bottom: 7px;
    border-radius: 25px;
    /*background-color: rgb(255, 123, 0);*/
    z-index: 0;
}